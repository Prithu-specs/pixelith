# Test fixtures

Deliberately empty of images.

Earlier development used a macOS system wallpaper as a sample photograph. That
file is Apple's copyrighted artwork and must not be redistributed, so tests now
synthesise their own inputs at runtime (see `test_engine.py`). Nothing here needs
to be downloaded for the suite to run.
